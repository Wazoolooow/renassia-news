# Redirections désactivées — SSL non disponible sur ce domaine

# Force le navigateur à toujours récupérer une version fraîche de index.html
<Files "index.html">
  Header set Cache-Control "no-cache, no-store, must-revalidate"
  Header set Pragma "no-cache"
  Header set Expires "0"
</Files>
